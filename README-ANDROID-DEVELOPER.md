# 📱 Инструкция для Android разработчика

## 📦 Что передать разработчику

Передай ему **всю папку `android-monitoring/`** со всеми файлами.

## 🎯 Что делает приложение

Приложение передает на сервер через WebSocket:

1. **📹 Видео поток** - кадры с камеры (передняя/задняя) в Base64
2. **📍 Геолокация** - GPS координаты в реальном времени
3. **🔋 Системная информация** - батарея, память, хранилище
4. **📱 Информация об устройстве** - модель, Android версия

## ⚙️ Настройка URL сервера

В файле `app/src/main/java/com/monitoring/service/MonitoringService.kt` на строке 34:

**Для локального тестирования:**
```kotlin
const val SERVER_URL = "ws://192.168.1.XXX:5000/ws/device"
```
*(Замени XXX на IP адрес компьютера где запущен backend)*

**Для продакшена:**
```kotlin
const val SERVER_URL = "wss://kelyastream.duckdns.org/ws/device"
```

## 🚀 Как запустить

1. Открой проект в Android Studio
2. Дождись синхронизации Gradle
3. Подключи устройство
4. Run → Run 'app'
5. В приложении нажми "Start Monitoring"

## ✅ Как проверить что работает

1. **Запусти backend:** `py start.py` (на порту 5000)
2. **Запусти frontend:** `cd frontend && npm start` (на порту 3000)
3. **Открой веб-панель:** http://localhost:3000
4. **Залогинься:** `admin` / `admin123`
5. **Проверь:**
   - Устройство появилось в списке ✅
   - Видео работает ✅
   - Геолокация работает ✅
   - Системная информация отображается ✅

## 📋 Формат данных

### Регистрация устройства
```json
{
  "type": "register",
  "device_info": {
    "id": "uuid",
    "name": "Имя устройства",
    "model": "Модель",
    "android_version": "Версия"
  }
}
```

### Кадр камеры
```json
{
  "type": "camera_frame",
  "camera": "front" | "back",
  "data": "base64-jpeg",
  "width": 640,
  "height": 480,
  "timestamp": 1234567890
}
```

### Геолокация
```json
{
  "type": "location_update",
  "lat": 55.7558,
  "lon": 37.6173,
  "accuracy": 10.5,
  "timestamp": 1234567890
}
```

### Системная информация
```json
{
  "type": "system_info",
  "data": {
    "battery_level": 85,
    "is_charging": true,
    "memory_usage": 50,
    "storage_usage": 60,
    "timestamp": 1234567890
  }
}
```

## 🔧 Настройки

### Изменить FPS камеры
В `MonitoringService.kt` строка 29:
```kotlin
private val cameraFpsLimit = 5 // Измени на нужное
```

### Изменить интервал системной информации
В `MonitoringService.kt` строка 109:
```kotlin
executor.scheduleAtFixedRate({...}, 10, 30, TimeUnit.SECONDS)
// Последний параметр - интервал в секундах
```

## 📞 Если что-то не работает

1. Проверь логи в Android Studio (LogCat)
2. Проверь логи backend сервера
3. Убедись что все разрешения выданы
4. Проверь что WebSocket соединение установлено

## 📚 Документация

- **Подробная инструкция:** `ANDROID-DEVELOPER-GUIDE.md`
- **Быстрый старт:** `QUICK-START-ANDROID.md`

---

**Удачи! 🚀**

