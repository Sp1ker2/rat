# 🚀 Быстрый старт - Android приложение

## 📋 Что нужно для тестирования

1. **Android Studio** (последняя версия)
2. **Android устройство** (Android 7.0+)
3. **Backend запущен** на компьютере

## ⚡ Быстрая настройка (5 минут)

### 1. Узнай IP адрес компьютера

**Windows:**
```powershell
ipconfig
```
Найди `IPv4 Address` (например: `192.168.1.100`)

**Mac/Linux:**
```bash
ifconfig | grep "inet "
```

### 2. Измени URL в Android коде

Открой файл: `android-monitoring/app/src/main/java/com/monitoring/service/MonitoringService.kt`

Найди строку 34:
```kotlin
const val SERVER_URL = "wss://kelyastream.duckdns.org/ws/device"
```

**Замени на:**
```kotlin
const val SERVER_URL = "ws://192.168.1.100:5000/ws/device"
```
*(Замени `192.168.1.100` на свой IP)*

### 3. Запусти backend

```powershell
py start.py
```

Должно быть: `INFO: Uvicorn running on http://127.0.0.1:5000`

### 4. Запусти frontend (опционально)

```powershell
cd frontend
npm start
```

### 5. Открой Android Studio

1. File → Open → выбери папку `android-monitoring`
2. Дождись синхронизации Gradle
3. Подключи телефон через USB
4. Включи "Отладка по USB"
5. Нажми Run (▶️) или Shift+F10

### 6. Запусти на телефоне

1. Открой приложение
2. Разреши все разрешения
3. Нажми "Start Monitoring"
4. Должно появиться уведомление

### 7. Проверь в браузере

Открой: **http://localhost:3000** (или http://192.168.1.100:3000 с телефона)

Логин: `admin` / `admin123`

Должна появиться карточка с твоим устройством!

## ✅ Чеклист проверки

- [ ] Backend запущен (порт 5000)
- [ ] Frontend запущен (порт 3000) - опционально
- [ ] IP адрес правильный в `MonitoringService.kt`
- [ ] Телефон и компьютер в одной Wi-Fi сети
- [ ] Приложение установлено на телефон
- [ ] Все разрешения выданы
- [ ] Мониторинг запущен
- [ ] Устройство видно в веб-панели
- [ ] Видео работает
- [ ] Геолокация работает

## 🐛 Частые проблемы

### Не подключается к серверу
- Проверь IP адрес
- Проверь, что backend запущен
- Проверь, что в одной Wi-Fi сети
- Проверь firewall (порт 5000 должен быть открыт)

### Не видно устройство в панели
- Проверь логи backend (должны быть сообщения о подключении)
- Проверь логи Android Studio (LogCat)
- Убедись, что WebSocket соединение установлено

### Нет видео
- Проверь разрешение на камеру
- Проверь, что камера работает
- Посмотри логи в LogCat

## 📱 Для тестирования на реальном сервере

Просто используй:
```kotlin
const val SERVER_URL = "wss://kelyastream.duckdns.org/ws/device"
```

И все! Приложение подключится к твоему серверу.

---

**Подробная инструкция:** Смотри `ANDROID-DEVELOPER-GUIDE.md`


